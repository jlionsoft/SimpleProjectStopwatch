<ui:ContentDialog x:Class="SimpleProjectStopwatch.Views.Dialogs.AEProject"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleProjectStopwatch.Views.Dialogs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:ui="http://schemas.modernwpf.com/2019"
                  xmlns:vm="clr-namespace:SimpleProjectStopwatch.ViewModels"
                  d:DataContext="{d:DesignInstance Type=vm:AEProjectViewModel}"
                  
                  PrimaryButtonText="Speichern"
                  DefaultButton="Primary"
                  PrimaryButtonCommand="{Binding Save}">
    <Grid>
        <ui:ScrollViewerEx>
            <ui:SimpleStackPanel>
                <AccessText Text="Projektname:"/>
                <TextBox Text="{Binding Project.Name}"
                     HorizontalAlignment="Stretch"/>
            </ui:SimpleStackPanel>
        </ui:ScrollViewerEx>
    </Grid>
</ui:ContentDialog>
